# npm's install-time audit POSTs the whole tree to
# registry.npmjs.org/-/npm/v1/security/advisories/bulk and hangs on this
# project (npm/cli#7383, #9804). Install stays offline to that endpoint;
# `npm run audit:deps` produces the report via OSV instead.
audit=false
